# Nothing in the deployed directory is private — Vercel serves this folder
# verbatim, so documentation must never live here. Docs belong in /docs.
*.md

# Assign Remind stays in the repo for later, but is not published while the
# product is held back: the professional-side module (nothing on the site
# imports it) and the client-facing My assignments page (reached only by the
# personal links that module used to hand out). Delete these lines to publish
# them again.
assets/assignremind.js
my-assignments.html
