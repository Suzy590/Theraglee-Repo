# Generated on demand by `build_documents.py --inline` — each file embeds the
# stylesheet and logo, so the folder is ~6 MB and is not worth versioning.
documents/_standalone/

# Scratch
.work/
__pycache__/
*.pyc
